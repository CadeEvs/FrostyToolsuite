<ctrl:FrostyDockableWindow x:Class="Frosty.Core.Windows.FrostyProfileSelectWindow"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
                           xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           mc:Ignorable="d"
                           Title="New Project" Height="600" Width="800" Loaded="ProfileSelectWindow_Loaded"
                           ResizeMode="NoResize" WindowStartupLocation="CenterScreen" SnapsToDevicePixels="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <ListBox x:Name="ConfigurationListView" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="true" Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
           
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding Path=Thumbnail}" Width="100" Height="100"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        
        <Grid Grid.Row="1" Background="{StaticResource ListBackground}">
            <Button x:Name="AddConfigButton" Content="Add" HorizontalAlignment="Right" Padding="12,0" Margin="0,6,66,6"/>
            <Button x:Name="SelectConfigButton" Content="Select" HorizontalAlignment="Right" Padding="12,0" Margin="0,6,6,6"/>

            <Button Content="Refresh" HorizontalAlignment="Left" Padding="12,0" Margin="6,6,0,6"/>
        </Grid>
    </Grid>
</ctrl:FrostyDockableWindow>
